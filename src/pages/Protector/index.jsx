import ProtectorConnection from './ProtectorConnection';

const Protector = () => {
  return (
    <div>
      <ProtectorConnection></ProtectorConnection>
    </div>
  );
};

export default Protector;
